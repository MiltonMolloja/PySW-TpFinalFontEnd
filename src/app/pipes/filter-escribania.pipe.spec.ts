import { FilterEscribaniaPipe } from './filter-escribania.pipe';

describe('FilterEscribaniaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEscribaniaPipe();
    expect(pipe).toBeTruthy();
  });
});
