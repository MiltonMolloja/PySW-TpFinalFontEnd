import { FilterNovedadPipe } from './filter-novedad.pipe';

describe('FilterNovedadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNovedadPipe();
    expect(pipe).toBeTruthy();
  });
});
